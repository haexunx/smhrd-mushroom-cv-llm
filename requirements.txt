# 웹 대시보드 UI와 사용자 입력/결과 출력을 제공한다.
streamlit

# YOLO 모델 로드, 객체 탐지, 바운딩 박스 시각화를 담당한다.
ultralytics

# 프롬프트-모델-출력 파서를 연결하는 LLM 실행 파이프라인이다.
langchain

# LangChain에서 OpenAI 채팅 모델을 호출하기 위한 연동 패키지이다.
langchain-openai

# 프로젝트의 .env 파일에서 OPENAI_API_KEY를 읽는다.
python-dotenv

# 업로드 이미지 디코딩 및 YOLO 결과 이미지 변환에 사용한다.
pillow

# 화면이 없는 서버에서도 Ultralytics의 이미지 처리가 가능하게 한다.
opencv-python-headless
